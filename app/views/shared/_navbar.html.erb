<nav class="navbar navbar-expand-lg bg-primary-subtle" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand btn btn-outline-light" href="/">Postulaciones</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to '💼Ofertas', offers_path, class: 'nav-link', aria: { current: 'page' } %>
        </li>
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= button_to '🔌Log Out', destroy_user_session_path, class: "nav-link", method: :delete %>
        </li>
          <% if current_user.isadmin? %>
          <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/admin/users/new">👥Create Account</a>
          </li>
          <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/messages/index">🔔Messages</a>
          </li>
         
          <%end%>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Iniciar sesión', new_user_session_path, class:'nav-link'%></li>          </li>
          <% if User.count == 0%>
          <li class="nav-item">
            <%= link_to 'Registro', new_user_registration_path, class: 'nav-link' %>
          </li>
          <% end %>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <div class="text-light">
          <% if user_signed_in? %>
            ✌Hola <%= current_user.name %>!
            <a data-bs-toggle="offcanvas" href="#cart">
              <i class="bi bi-cart-plus"></i>
            </a>
          <% else %>
            👨‍💻Visita
          <% end %>
        </div>
      </form>
    </div>
  </div>
</nav>
